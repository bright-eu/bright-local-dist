# bright-local-dist

Public home for `bright-local` release artifacts.

The source code lives in the private repo [`bright-eu/bright-local`](https://github.com/bright-eu/bright-local). Release tarballs are published here so Homebrew can download them without authentication.

Install:

```bash
brew install bright-eu/tap/bright-local
```
